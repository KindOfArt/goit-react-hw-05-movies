import React from 'react';
// import PropTypes from 'prop-types';

const Status = ({ status }) => {
  return (
    <div>
      <span>{status}</span>
    </div>
  );
};

Status.propTypes = {};

export default Status;
